import { type TypeRoundingConversion } from "@src/types";

const ROUNDING_CONVERSION: TypeRoundingConversion = {
  REAL_RATE: 3,
  MIN_VALUE: 1,
  SLICE_MIN: 2,
  SLICE_MAX: 6,
  VIEW_RESULT: 9
};

export { ROUNDING_CONVERSION };
